from setuptools import setup,find_packages
 # it is related to __init__.py (local package) each folder for packages
setup(
  name="us-visa",
  version="0.0.0",
  author="Ravi Teja",
  author_email="sureraviteja23@gmail.com",
  packages=find_packages()
)